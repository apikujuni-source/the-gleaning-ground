name: Debug text flow v3

on:
  push:
    branches: [agent/fix-text-flow-v3-single-line]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
      - run: node scripts/validate-divine-blueprint-build.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: text-flow-v3-preview
          path: |
            _site/divine-blueprint-site/**/*.html
            _site/divine-blueprint-site/assets/styles.css
            _site/divine-blueprint-site/assets/text-flow-audit.json
          retention-days: 1
